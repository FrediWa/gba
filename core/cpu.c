#include "cpu.h"

void fetch(void)
{
    // unsigned char buffer[2]; 
    // while (fread(buffer, sizeof(unsigned char), 2, file) == 2) {
    //  printf("%02x: %02x\n", buffer[0], buffer[1]);
    // }
}